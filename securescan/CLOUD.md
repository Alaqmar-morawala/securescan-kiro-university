# Bonus 1 — Kiro Web / cloud sessions / cloud configuration (paid plans only)

Status: **NOT claimed — free subscription.**

- This repo was built with `kiro-cli 2.20.1` (logged in with Builder ID,
  `2403051050600@paruluniversity.ac.in`, free plan).
- `kiro-cli --cloud` / cloud sessions + cloud configuration require
  Pro/Pro+/Pro Max/Power (US East N. Virginia) per the lesson text.
- Attempted: `kiro-cli chat --cloud` is available as a flag, but launching a
  cloud sandbox on a free account is not entitled; Kiro Web requires a paid
  subscription to run. No cloud session was used for this build — all work is
  local CLI so judges can verify offline.
- If you upgrade before Oct 5: upload `.kiro/` via Kiro Web → Settings → Sync
  (cloud configuration), run one refactor/test pass in a cloud session, and
  mention the session ID in the entry form to claim +250.

Lesson text: "Push through a broad refactor or upgrade. Start a cloud session
from the IDE or CLI ... Upload your local `.kiro` configuration to cloud
configuration, and that cloud work uses the same setup you've built locally."
