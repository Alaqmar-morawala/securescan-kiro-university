# Runtime deps for Vercel (@vercel/python installs from this file).
# Pinned ranges that work on Vercel's Python 3.12/3.13 runtime.
Django>=5.0,<6.0
reportlab>=4.0
whitenoise>=6.6
# Only needed by the real ZAP path (scanner/zap_client.py::ZapClient);
# imported lazily so mock mode runs without it.
requests>=2.31
